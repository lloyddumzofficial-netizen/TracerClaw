export const metadata = {
  title: "Premium Sports Jersey Mockup Studio",
  description: "Turn exact front, back, sleeve, collar and cuff artwork into a premium five-view sportswear campaign set.",
  robots: { index: false, follow: false },
};

export default function MockupStudioLayout({ children }) {
  return children;
}
